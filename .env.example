# ==============================================================================
# OpenClaw Code Reconstruction Workflow - Environment Variables Configuration
# ==============================================================================

# 1. 待反构的微服务源代码根目录路径
# 指向您本地包含各微服务（前端/后端）代码的父级文件夹（此处使用通用相对路径作为示例）。
CODE_ROOT="./src"

# 2. 规则模板目录路径
# 指向本仓库中的 commands 文件夹所在路径。
# 智能体将在此目录下读取 01-prd-rules, 02-api-rules, 03-design-rules 文件夹中的规则。
RULES_ROOT="./commands"

# 3. 反构生成的知识文档输出根目录路径
# 反构生成的功能清单、PRD、接口和详细设计文档都将输出至此目录。
OUTPUT_ROOT="./knowledge"
